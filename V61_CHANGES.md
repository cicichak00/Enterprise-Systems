# v61 企业号员工离职收益回收规则

## H5 已完成

- 企业主把员工从“在职”改为“离职”并保存时，新增离职二次确认。
- 员工管理弹层直接展示当前待提现金额；切换为离职后同步展示回收提示，并将保存按钮改为“确认离职并保存”。
- 二次确认改为定制交互层，明确展示员工、余额以及员工负流水、企业主正流水、回收统计三步处理结果。
- 弹窗展示员工当前待提现金额，文案为：`当前员工余额：xx元。员工离职后，该笔待提现金额将回收至企业主收益，确认将员工状态变更为离职？`
- 待提现金额大于 0 时不做前端拦截，确认后仍提交离职状态。
- 企业主“我的”收益卡新增“离职员工回收金额”。
- 企业主订单卡优先展示订单拉取时固化的 `employment_status_snapshot` 在职/离职状态。
- 员工详情/列表、企业主收益卡、订单记录及钱包流水补充了对应字段类型。

## 正式接口契约

### 员工列表与详情

`GET /api/v1.enterprise.Staff/list` 和 `GET /api/v1.enterprise.Staff/detail`：

- 返回 `pending_withdrawal_amount`，单位为元，表示员工当前待提现金额。

### 员工离职

`POST /api/v1.enterprise.Staff/config` 的 `status` 从 `1` 变为 `0` 时，服务端必须在同一数据库事务内：

1. 锁定员工关系和员工/企业主钱包，重新读取员工在职状态及待提现金额。
2. 不再因待提现金额大于 0 拒绝离职。
3. 将员工状态更新为离职。
4. 员工侧生成一笔“离职收益回收”流水，`amount_signed` 为负数。
5. 企业主侧生成一笔等额“离职员工收益回收”流水，`amount_signed` 为正数。
6. 员工待提现金额扣减至 0，企业主离职回收累计金额增加相同数额。
7. 两笔流水共用唯一 `recovery_pair_no`，并记录 `related_staff_id`，便于对账和保证幂等。

若员工已经离职，重复请求不得再次回收；任一步失败必须整体回滚。

### 企业主“我的”

`GET /api/v1.enterprise.Mine/index` 的 `data.revenue_card`：

- 新增 `resigned_staff_recovery_amount`，单位为元，统计已离职员工被回收的待提现金额累计值。

### 订单定时拉取

订单拉取任务处理每笔订单前必须按企业、用户查询员工关系，并固化：

- `employment_status_snapshot`: `1` 在职，`0` 离职。
- 仅在员工在职时按员工分佣规则计入员工待提现收益。
- 员工已离职时不得再向该员工钱包入账；收益按服务端既定的离职订单归属规则计入企业主。
- 状态查询、订单幂等校验、收益入账和流水写入必须放在一致的事务边界内，避免离职与订单拉取并发造成重复或错账。

## 验收重点

- 待提现金额为 0 和大于 0 时均可离职，且都出现二次确认。
- 取消弹窗不提交；确认后只回收一次。
- 员工负流水、企业主正流水金额完全一致，且可通过 `recovery_pair_no` 配对。
- 离职回收累计金额等于全部有效企业主正向回收流水之和。
- 离职操作与订单定时拉取并发时，不出现员工离职后新增待提现金额。
