<template>
	<cl-page>
		<view class="guide">
			<view class="guide-intro"><text class="guide-title">开始你的第一个任务</text><text class="guide-description">选任务 → 参与 → 回填 → 查收益</text></view>
			<view v-for="(step, index) in steps" :key="step.title" class="guide-step">
				<view class="guide-step-head"><text class="guide-number">{{ index + 1 }}</text><text class="guide-step-title">{{ step.title }}</text></view>
				<text class="guide-copy">{{ step.content }}</text>
			</view>
			<view class="guide-note"><text class="guide-step-title">按习惯查看任务</text><text class="guide-copy">首页可切换“双列”和“列表”，选择会保存在当前设备。上方分类可以筛选任务。</text></view>
			<view class="guide-note"><text class="guide-step-title">找不到任务或收益？</text><text class="guide-copy">先检查分类和时间筛选。任务要求、回填方式和收益更新时间以具体项目说明为准；仍有疑问，请联系企业主。</text></view>
		</view>
	</cl-page>
</template>
<script lang="ts" setup>
const steps = [
	{ title: "选择任务", content: "在首页按分类查找项目，点击任务名称查看要求、规则和结算说明。" },
	{ title: "参与任务", content: "点击“参与任务”，按项目页面的说明领取内容或完成操作。需要报备账号的项目，可在“我的 → 账号报备”中管理账号。" },
	{ title: "提交回填", content: "完成后，点击项目的“立即回填”，按页面要求填写并提交。部分项目没有回填入口，请按项目说明操作。已有记录可在“我的 → 我的词库”查看。" },
	{ title: "查看收益", content: "打开底部“收益台”，查看累计、待结算和已结算收益。通过时间筛选查看对应订单，数据以系统更新为准。" }
];
</script>
<style lang="scss" scoped>
@import "@/styles/owner-workbench.scss";
.guide { background: $background; min-height: 100vh; padding: 14px 14px calc(24px + env(safe-area-inset-bottom)); box-sizing: border-box; max-width: 800px; margin: auto; }
.guide-intro { @include feature; padding: 20px 16px; margin-bottom: 14px; }
.guide-title { @include title; display: block; }
.guide-description { display: block; margin-top: 10px; color: $primary; font-size: 13px; line-height: 22px; }
.guide-step, .guide-note { @include card; padding: 16px; margin-bottom: 10px; }
.guide-step-head { display: flex; flex-direction: row; align-items: center; gap: 10px; margin-bottom: 10px; }
.guide-number { background: #eef2ff; color: $primary; font-size: 14px; line-height: 28px; width: 28px; text-align: center; border-radius: 8px; font-weight: 600; }
.guide-step-title { @include section; display: block; }
.guide-copy { display: block; font-size: 13px; line-height: 23px; color: #71798b; }
.guide-note .guide-copy { margin-top: 8px; }
</style>
